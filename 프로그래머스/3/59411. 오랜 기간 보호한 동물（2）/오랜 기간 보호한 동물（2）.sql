-- 코드를 입력하세요
SELECT
    ANIMAL_ID,
    NAME
FROM
    (SELECT
    DATEDIFF(OUTS.DATETIME,INS.DATETIME) AS DAYS,
    INS.ANIMAL_ID,
    INS.NAME
FROM
    ANIMAL_INS INS

JOIN
    ANIMAL_OUTS OUTS

ON
    OUTS.ANIMAL_ID = INS.ANIMAL_ID

ORDER BY
    DAYS DESC

LIMIT 2) as Sub