-- 코드를 작성해주세요
SELECT
    SUM(SCORE) AS SCORE,
    HR_GRADE.EMP_NO,
    HR_EMPLOYEES.EMP_NAME,
    HR_EMPLOYEES.POSITION,
    HR_EMPLOYEES.EMAIL
FROM
    HR_GRADE
    
JOIN
    HR_EMPLOYEES
ON
    HR_GRADE.EMP_NO = HR_EMPLOYEES.EMP_NO

GROUP BY
    EMP_NO, YEAR

ORDER BY
    SCORE DESC
LIMIT 1